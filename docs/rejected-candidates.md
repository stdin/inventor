# Rejected Candidates

An append-only log of every invention candidate dispositioned as `reject` by `$inventor-disposition`. Its only job is to stop the same crowded concept from being rediscovered and re-litigated from scratch. Keep entries short — the full reasoning lives on the card itself before it's archived.

| Date | Candidate | Technical problem | Rejected because | Card |
| --- | --- | --- | --- | --- |
| <YYYY-MM-DD> | <name> | <one line> | <gate that failed, or "self-prior-art" / "existing art"> | `docs/cards/NNNN-slug.md` |
